Host *
  # Compression
  Compression = yes
  CompressionLevel = 9
  #Initial TimeOut
  #ConnectTimeout = 600
  # Multiplexing over TCP
  ControlMaster = auto
  ControlPath = /tmp/ssh_mux__%r@%h_%p
  ControlPersist = 1h
  # Default Options
  User = mohammad
  Port = 22
  # Auth
  PreferredAuthentications = publickey,gssapi-with-mic,hostbased,keyboard-interactive,password
  # Forward Local Keys to Remote Host, to hop from Host-to-Host
  #ForwardAgent = yes
  # Keep Connection Alive
  ServerAliveInterval = 30
  # ASCII Key
  VisualHostKey = yes
